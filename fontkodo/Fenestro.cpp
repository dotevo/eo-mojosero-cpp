#include <mojosero/Fenestro.hpp>

using namespace Mojosero;

Fenestro::Fenestro (std::string nomo, LKesto<int> lkesto, int ekrano) {
	/*screen.window = SDL_CreateWindow(nomo, lkesto.getX, screen.bounds.y,	screen.bounds.w, screen.bounds.h,
			SDL_WINDOW_BORDERLESS
			);
	a_bildigilo = SDL_CreateRenderer( a_fenestro, 0, SDL_RENDERER_ACCELERATED );*/
}
